version: '3'

services:
  db:
    image: mongodb
    env_file:
      - .env
  web_app:
    build:
      dockerfile: sentence_similarity/Dockerfile
      context: sentence_similarity
    image: sentense-similarity
    env_file:
      - .env
    depends_on:
      - db
    ports:
      - "8000:8000"